//https://tolerant-walrus-74.clerk.accounts.dev

const authconfig= {
    providers: [
      {
        domain: "https://tolerant-walrus-74.clerk.accounts.dev",
        applicationID: "convex",
      },
    ]
  };

export default authconfig;